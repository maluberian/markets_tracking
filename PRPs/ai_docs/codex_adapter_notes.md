# codex Adapter Notes

The AI documentation in this directory originated from the codex CLI workflow. When using these materials with OpenAI codex:

- Treat any reference to "codex" or `.codex/` as referring to `codex` and `.codex/` in this repository.
- Substitute codex CLI commands where the docs mention the `codex` CLI.
- Reapply the validation-first mindset detailed in the docs; the concrete tooling commands have already been ported to codex in `.codex/commands/`.

Use this file as a reminder while the underlying reference material is gradually updated for codex-specific features.
